# Ralph Runner Instructions

You are running one opt-in Ralph iteration for `astrograph`.

## Run Context

- Ralph run directory: `.ralph/mcp-v1-hard-switch`
- PRD title: `MCP v1 hard-switch`
- PRD branch: `feat/mcp-v1-hard-switch`
- Current git branch: `feat/mcp-v1-hard-switch`
- Active story: `MCPV1-3 - Enforce strict MCP v1 response envelopes`
- Active story status: `pending`

## Files To Read First

1. `AGENTS.md`
2. Relevant `.agents/rules/` files
3. `.ralph/mcp-v1-hard-switch/prd.json`
4. `.ralph/mcp-v1-hard-switch/progress.txt`
5. Any closer `AGENTS.md` files in directories you edit

## Story Notes

Implement Task 3 from specs/implementation/mcp-v1-hard-switch-plan.md. Dispatch must return success and error envelopes with ok, data, meta, error where appropriate. Move token estimate/freshness metadata into envelope meta. Normalize thrown errors. Add focused tests.

## Story Queue Snapshot

- MCPV1-1: Lock explicit MCP v1 contract in specs [status=done, priority=high]
- MCPV1-2: Register explicit MCP v1 retrieval tools and remove query_code from MCP [status=done, priority=high]
- MCPV1-3: Enforce strict MCP v1 response envelopes [status=pending, priority=high]
- MCPV1-4: Lock MCP v1 tests and parser metadata assertions [status=pending, priority=medium]
- MCPV1-5: Release hardening and final rollout checks [status=pending, priority=medium]

## Recent Progress Tail

```text
## Run Log

## Run Log
- Initialized MCP v1 hard-switch Ralph lane from specs/implementation/mcp-v1-hard-switch-plan.md.
- Phase A tree-sitter parser cutover was already committed separately as e46ef97.
- Completed MCPV1-1: locked the explicit MCP v1 docs contract and marked Task 1 Step 2 complete.
- Files changed: specs/raw/astrograph_jcodemunch_agent_spec.md, specs/api-design/mcp-tools.md, specs/architecture/adrs.md, specs/implementation/README.md, specs/README.md, specs/implementation/mcp-v1-hard-switch-plan.md, .ralph/mcp-v1-hard-switch/prd.json, .ralph/mcp-v1-hard-switch/progress.txt.
- Checks run: pnpm type-lint; pnpm exec vitest run tests/interface.test.ts tests/engine-contract.test.ts.
- Reusable learning: `query_code` remains a non-MCP compatibility concept in some docs/tests, but MCP v1 docs should describe only the explicit retrieval tools plus strict `ok`/`data`/`meta`/`error` envelopes and `toolVersion: "1"` metadata.
- Next step: MCPV1-2 should implement the registered explicit MCP tools and remove `query_code` from `MCP_TOOL_DEFINITIONS` without changing the non-MCP `queryCode` API.
- Completed MCPV1-2: removed `query_code` from MCP registration while keeping CLI/library `queryCode` available, and registered `search_symbols`, `get_symbol_source`, `get_context_bundle`, and `get_ranked_context` with strict argument schemas plus `toolVersion: "1"` metadata.
- Files changed: src/command-registry.ts, src/config.ts, src/language-registry.ts, src/mcp-contract.ts, src/scripts/install.ts, src/types/config.ts, tests/interface.test.ts, tests/engine-contract.test.ts, specs/implementation/mcp-v1-hard-switch-plan.md, .ralph/mcp-v1-hard-switch/prd.json, .ralph/mcp-v1-hard-switch/progress.txt.
- Checks run: pnpm exec vitest run tests/interface.test.ts tests/engine-contract.test.ts; pnpm type-lint.
- Reusable learning: MCP and install tool lists derive from `mcpToolName` on `COMMAND_REGISTRY`; keeping the explicit retrieval entries ordered `search_symbols`, `get_symbol_source`, `get_context_bundle`, `get_ranked_context` avoids the runtime alignment guard in `src/mcp-contract.ts`.
- Next step: MCPV1-3 should wrap all MCP dispatch success and failure paths in strict `ok`/`data`/`meta`/`error` envelopes and move token/freshness data into envelope metadata.
```

## Required Working Style

- Implement only the single active story for this iteration.
- Use repo-native conventions: `pnpm`, workspace-scoped checks first, and
  `jcodemunch` first for code navigation, with Astrograph
  (`@mortenbroesby/astrograph`; compatibility bin `ai-context-engine`) kept
  available as the secondary path until the repo is ready to switch fully.
- Keep the worktree coherent and avoid unrelated cleanup.
- Update `.ralph/mcp-v1-hard-switch/progress.txt` by appending a short iteration log with:
  story completed or blocked
  files changed
  checks run
  reusable learnings or gotchas
- Leave a short next-step note when partial work remains so the next iteration
  can resume without rediscovery.
- Update `.ralph/mcp-v1-hard-switch/prd.json` only if the story is fully complete and the
  listed checks passed.
- Add reusable directory-specific learnings to nearby `AGENTS.md` files only if
  they are truly durable and worth loading in future sessions.

## Checks To Run

- pnpm type-lint
- pnpm exec vitest run tests/interface.test.ts tests/engine-contract.test.ts

## Commit Policy

Do not commit automatically.
Leave the worktree ready for human review after updating `prd.json` and `progress.txt`.

## Guardrails

- Do not switch branches automatically.
- Do not bypass approvals or sandboxing.
- Do not mark the story passed if verification is incomplete.
- If blocked, leave a clear blocker note in `progress.txt` instead of guessing.
